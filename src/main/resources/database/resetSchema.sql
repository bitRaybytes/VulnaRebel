-- this schema could be used to reset the database
-- for now, it is not responsible for doing anything

-- f.e. drop entire database and create a new fresh one
-- at runtime in the docker containerization.

-- create table users
-- (
--     id INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
--     username VARCHAR(25),
--     password VARCHAR(25),
--     role VARCHAR(25)
-- )